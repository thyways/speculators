# Research Log

Chronological record of research decisions and actions. Append-only.

| # | Date | Type | Summary |
|---|------|------|---------|
| 1 | 2026-08-05 | bootstrap | Resumed the Parser2 DFlash effort. The source is a 12.2 GB ShareGPT-style multimodal JSONL, target/verifier is `Infinity-Parser2-2B-2604`, pilot is 800k, and final validation is a nested 1.5M sample. Existing uncommitted code already implements deterministic ranked sampling and strict preprocessing, but its regeneration default incorrectly points to `Infinity-Parser2-Flash`, while its training launcher uses Muon + linear and causal sliding attention. H1/H2 lock the required corrections before GPU execution. |
| 2 | 2026-08-05 | invalid-run | The first real smoke passed target serving and exact multimodal token alignment, but revealed that `max_steps=10` only broke the inner epoch loop. Training continued with one update in each later epoch and reached 15 updates. The run was stopped, archived, and excluded from evidence. |
| 3 | 2026-08-05 | implementation | Added a run-level `max_steps` termination guard plus a cross-epoch regression test in commit `27037e3`; 60 focused unit tests passed while all eight H100s remained occupied by the keepalive process. |
| 4 | 2026-08-05 | confirmatory-result | Clean smoke `20260805T023908Z-425140` passed all locked H2 criteria: exact 3,961-token multimodal round-trip, four hidden-state layers of width 2,048, six-rank DDP steps 0--9 only, finite losses, no mismatch/OOM/NaN, and a readable checkpoint. Final validation loss was 3.809215 and checkpoint SHA-256 was `9004c715969c0514db87c5dce5d6a5e3d348733276c015efa04859c911a77b26`. Direction is DEEPEN to the nested 800k pilot. |
| 5 | 2026-08-05 | protocol-amendment | Before any scaled generation request, changed execution order to generate and preprocess the deterministic 1.5M corpus once. The 800k pilot will be a loader-enforced prefix selected before its 99/1 split, while both scales share the 1.5M-derived vocabulary mapping. This saves a duplicate merge/tokenization/vocab pass without reducing the teacher tokens required for the final corpus. Regeneration remains temperature 0, top-p 1, seed 42, and thinking disabled. |
| 6 | 2026-08-05 | protocol-clarification | The user clarified that only teacher regeneration must be shared. Paused the just-started full wrapper after atomic segments had begun, before any scaled preprocessing. The final protocol regenerates the 1.5M stage once, then runs an 800k local merge/tokenization/mapping pipeline with zero new teacher requests; 1.5M preprocessing/mapping remains separate. This supersedes the shared-mapping/loader-prefix detail in entry 5. |
| 7 | 2026-08-05 | experiment-start | Under protocol commit `7a97c19`, restarted generation in full-stage generation-only mode against eight healthy one-GPU `Infinity-Parser2-2B-2604` replicas. The process resumed six committed 128-event segments from the paused wrapper; no 800k-specific teacher generation was launched. |
| 8 | 2026-08-05 | exploratory-result | Regeneration concurrency A/B selected 32 requests per endpoint. The clean cache-disabled rate was 43.57 records/s versus 28.05 at concurrency 16 and about 11.17 at concurrency 4. Default vLLM MM caching caused an `Expected a cached item for mm_hash` race and HTTP 500 at higher concurrency; restarting with `--mm-processor-cache-gb 0` removed the failure. The selected 4,096-record window had zero errors, no fatal log, and 8/8 healthy replicas. Projected generation time is about 9.7 hours, operationally 10--13 hours. |
